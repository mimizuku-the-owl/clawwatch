# ClawWatch Configuration
# Copy this file to .env.local for local development or .env for Docker

# =============================================================================
# REQUIRED: Gateway Connection
# =============================================================================

# Your Clawdbot gateway URL (WebSocket endpoint)
GATEWAY_URL=http://127.0.0.1:18789

# Gateway authentication token (get this from your gateway config)
GATEWAY_TOKEN=your_gateway_token_here

# =============================================================================
# OPTIONAL: Collector Settings
# =============================================================================

# Directory where agent session transcripts are stored
# Default: /home/moltbot/.clawdbot/agents
SESSIONS_DIR=/home/moltbot/.clawdbot/agents

# How often to poll for new data (milliseconds)
# Default: 60000 (60 seconds)
SESSION_POLL_INTERVAL=60000

# =============================================================================
# OPTIONAL: Convex Database
# =============================================================================

# URL for the Convex backend.
# Self-hosted default: http://127.0.0.1:3210
# Convex Cloud: https://YOUR_DEPLOYMENT.convex.cloud
CONVEX_URL=http://127.0.0.1:3210

# For the webapp build (Vite)
# Self-hosted default: http://127.0.0.1:3210
# Convex Cloud: https://YOUR_DEPLOYMENT.convex.cloud
VITE_CONVEX_URL=http://127.0.0.1:3210

# Admin key for Convex (only needed for schema deployment)
# Get this by running: docker compose -f docker-compose.selfhosted.yml exec convex-backend ./generate_admin_key.sh
CONVEX_SELF_HOSTED_ADMIN_KEY=

# =============================================================================
# OPTIONAL: Docker overrides
# =============================================================================

# Docker Hub namespace for cloud compose (default: clawwatch)
DOCKERHUB_NAMESPACE=clawwatch

# Override Convex cloud origin (advanced)
CONVEX_CLOUD_ORIGIN=http://127.0.0.1:3210

# Override site origin (advanced)
CONVEX_SITE_ORIGIN=http://127.0.0.1:3211

# Document retention in seconds (default: 2 days)
DOCUMENT_RETENTION_DELAY=172800

# Rust log level for Convex backend
RUST_LOG=info
